// Badges are now composed directly inside ProductShowcase to keep
// their positioning relative to the cheesecake circle. Kept as a
// no-op component so existing imports/usages don't need to change.
export function ProductBadges() {
  return null
}
